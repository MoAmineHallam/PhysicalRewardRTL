Write a Verilog module named `sub14b` that subtracts two 14-bit unsigned numbers, registered, with a 15-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [13:0] a, b
  output [14:0] result
