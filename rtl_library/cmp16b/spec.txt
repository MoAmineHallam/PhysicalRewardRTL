Write a Verilog module named `cmp16b`, a registered 16-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [15:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
