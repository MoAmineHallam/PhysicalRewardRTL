Write a Verilog module named `sub5b` that subtracts two 5-bit unsigned numbers, registered, with a 6-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [4:0] a, b
  output [5:0] result
