Write a Verilog module named `lfsr6_2`, a 6-bit Galois LFSR with feedback tap mask 0x21 and seed 0x3f (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [5:0] state.
