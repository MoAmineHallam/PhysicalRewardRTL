Write a Verilog module named `cmp4b`, a registered 4-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [3:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
