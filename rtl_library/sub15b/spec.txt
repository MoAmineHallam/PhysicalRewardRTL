Write a Verilog module named `sub15b` that subtracts two 15-bit unsigned numbers, registered, with a 16-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [14:0] a, b
  output [15:0] result
