Write a Verilog module named `mod14_counter` that is a free-running modulo-14 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [3:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,13,0,... wrapping at 14.
