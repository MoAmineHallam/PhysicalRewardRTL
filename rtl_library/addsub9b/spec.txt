Write a Verilog module named `addsub9b` that adds or subtracts (sub input selects) two 9-bit unsigned numbers, registered, with a 10-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [8:0] a, b
  input sub (0=add,1=sub)
  output [9:0] result
