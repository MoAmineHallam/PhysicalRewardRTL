Write a Verilog module named `add3b` that adds two 3-bit unsigned numbers, registered, with a 4-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [2:0] a, b
  output [3:0] result
