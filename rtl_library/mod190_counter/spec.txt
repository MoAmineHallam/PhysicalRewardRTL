Write a Verilog module named `mod190_counter` that is a free-running modulo-190 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [7:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,189,0,... wrapping at 190.
