Write a Verilog module named `mod38_counter` that is a free-running modulo-38 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [5:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,37,0,... wrapping at 38.
