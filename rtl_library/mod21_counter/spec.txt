Write a Verilog module named `mod21_counter` that is a free-running modulo-21 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [4:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,20,0,... wrapping at 21.
