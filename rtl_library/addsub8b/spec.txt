Write a Verilog module named `addsub8b` that adds or subtracts (sub input selects) two 8-bit unsigned numbers, registered, with a 9-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [7:0] a, b
  input sub (0=add,1=sub)
  output [8:0] result
