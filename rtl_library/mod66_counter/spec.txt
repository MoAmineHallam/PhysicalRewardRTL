Write a Verilog module named `mod66_counter` that is a free-running modulo-66 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [6:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,65,0,... wrapping at 66.
