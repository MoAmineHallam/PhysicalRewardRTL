Write a Verilog module named `cmp13b`, a registered 13-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [12:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
