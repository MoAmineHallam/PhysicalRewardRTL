Write a Verilog module named `addsub6b` that adds or subtracts (sub input selects) two 6-bit unsigned numbers, registered, with a 7-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [5:0] a, b
  input sub (0=add,1=sub)
  output [6:0] result
