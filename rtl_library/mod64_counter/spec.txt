Write a Verilog module named `mod64_counter` that is a free-running modulo-64 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [5:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,63,0,... wrapping at 64.
