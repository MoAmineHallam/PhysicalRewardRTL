Write a Verilog module named `lfsr14_12`, a 14-bit Galois LFSR with feedback tap mask 0x2015 and seed 0x2abc (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [13:0] state.
