Write a Verilog module named `lfsr9_7`, a 9-bit Galois LFSR with feedback tap mask 0x110 and seed 0x1aa (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [8:0] state.
