Write a Verilog module named `rotate_rsh16`, a registered 16-bit right rotate rotator.

Ports:
  input clk, rst_n
  input [15:0] data
  input [3:0] shamt
  output [15:0] out

Behavior: out <= data right-rotate by shamt.
