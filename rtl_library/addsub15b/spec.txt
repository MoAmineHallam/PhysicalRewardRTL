Write a Verilog module named `addsub15b` that adds or subtracts (sub input selects) two 15-bit unsigned numbers, registered, with a 16-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [14:0] a, b
  input sub (0=add,1=sub)
  output [15:0] result
