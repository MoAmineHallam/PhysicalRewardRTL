Write a Verilog module named `addsub13b` that adds or subtracts (sub input selects) two 13-bit unsigned numbers, registered, with a 14-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [12:0] a, b
  input sub (0=add,1=sub)
  output [13:0] result
