Write a Verilog module named `addsub14b` that adds or subtracts (sub input selects) two 14-bit unsigned numbers, registered, with a 15-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [13:0] a, b
  input sub (0=add,1=sub)
  output [14:0] result
