Write a Verilog module named `mod112_counter` that is a free-running modulo-112 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [6:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,111,0,... wrapping at 112.
