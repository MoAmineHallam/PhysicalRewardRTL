Write a Verilog module named `mod76_counter` that is a free-running modulo-76 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [6:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,75,0,... wrapping at 76.
