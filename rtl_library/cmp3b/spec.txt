Write a Verilog module named `cmp3b`, a registered 3-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [2:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
