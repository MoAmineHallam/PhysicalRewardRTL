Write a Verilog module named `addsub7b` that adds or subtracts (sub input selects) two 7-bit unsigned numbers, registered, with a 8-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [6:0] a, b
  input sub (0=add,1=sub)
  output [7:0] result
