Write a Verilog module named `cmp9b`, a registered 9-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [8:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
