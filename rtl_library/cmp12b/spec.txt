Write a Verilog module named `cmp12b`, a registered 12-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [11:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
