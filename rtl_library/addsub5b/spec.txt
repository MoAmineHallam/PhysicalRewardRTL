Write a Verilog module named `addsub5b` that adds or subtracts (sub input selects) two 5-bit unsigned numbers, registered, with a 6-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [4:0] a, b
  input sub (0=add,1=sub)
  output [5:0] result
