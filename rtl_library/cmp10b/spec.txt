Write a Verilog module named `cmp10b`, a registered 10-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [9:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
