Write a Verilog module named `add14b` that adds two 14-bit unsigned numbers, registered, with a 15-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [13:0] a, b
  output [14:0] result
