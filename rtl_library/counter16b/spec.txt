Write a Verilog module named `counter16b`, a 16-bit free-running binary up-counter.

Ports:
  input clk
  input rst_n (active-low sync reset)
  output [15:0] count

Behavior:
  - Registered. On !rst_n count<=0. Else count<=count+1 (wraps at 2^16).
