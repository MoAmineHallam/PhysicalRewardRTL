Write a Verilog module named `lfsr8_4`, a 8-bit Galois LFSR with feedback tap mask 0x8e and seed 0xac (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [7:0] state.
