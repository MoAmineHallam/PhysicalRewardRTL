Write a Verilog module named `cmp11b`, a registered 11-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [10:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
