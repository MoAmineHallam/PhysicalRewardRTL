Write a Verilog module named `cmp14b`, a registered 14-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [13:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
