Write a Verilog module named `cmp15b`, a registered 15-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [14:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
