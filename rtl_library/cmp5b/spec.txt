Write a Verilog module named `cmp5b`, a registered 5-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [4:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
