Write a Verilog module named `rotate_lsh6`, a registered 6-bit left rotate rotator.

Ports:
  input clk, rst_n
  input [5:0] data
  input [2:0] shamt
  output [5:0] out

Behavior: out <= data left-rotate by shamt.
