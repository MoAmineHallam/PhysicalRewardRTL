Write a Verilog module named `addsub2b` that adds or subtracts (sub input selects) two 2-bit unsigned numbers, registered, with a 3-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [1:0] a, b
  input sub (0=add,1=sub)
  output [2:0] result
