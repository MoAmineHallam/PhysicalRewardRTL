Write a Verilog module named `lfsr5_1`, a 5-bit Galois LFSR with feedback tap mask 0x12 and seed 0x1f (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [4:0] state.
