Write a Verilog module named `mod79_counter` that is a free-running modulo-79 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [6:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,78,0,... wrapping at 79.
