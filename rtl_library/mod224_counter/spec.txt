Write a Verilog module named `mod224_counter` that is a free-running modulo-224 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [7:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,223,0,... wrapping at 224.
