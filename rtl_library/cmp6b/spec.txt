Write a Verilog module named `cmp6b`, a registered 6-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [5:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
