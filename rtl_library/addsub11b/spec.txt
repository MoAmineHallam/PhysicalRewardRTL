Write a Verilog module named `addsub11b` that adds or subtracts (sub input selects) two 11-bit unsigned numbers, registered, with a 12-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [10:0] a, b
  input sub (0=add,1=sub)
  output [11:0] result
