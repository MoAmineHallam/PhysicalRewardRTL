Write a Verilog module named `mod6_counter` that is a free-running modulo-6 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [2:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,5,0,... wrapping at 6.
