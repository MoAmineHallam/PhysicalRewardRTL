Write a Verilog module named `mod3_counter` that is a free-running modulo-3 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [1:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,2,0,... wrapping at 3.
