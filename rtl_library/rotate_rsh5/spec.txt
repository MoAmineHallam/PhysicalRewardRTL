Write a Verilog module named `rotate_rsh5`, a registered 5-bit right rotate rotator.

Ports:
  input clk, rst_n
  input [4:0] data
  input [2:0] shamt
  output [4:0] out

Behavior: out <= data right-rotate by shamt.
