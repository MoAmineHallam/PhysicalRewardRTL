Write a Verilog module named `lfsr16_14`, a 16-bit Galois LFSR with feedback tap mask 0xb400 and seed 0xace1 (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [15:0] state.
