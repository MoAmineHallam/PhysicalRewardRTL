Write a Verilog module named `mod166_counter` that is a free-running modulo-166 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [7:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,165,0,... wrapping at 166.
