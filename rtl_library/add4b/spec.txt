Write a Verilog module named `add4b` that adds two 4-bit unsigned numbers, registered, with a 5-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [3:0] a, b
  output [4:0] result
