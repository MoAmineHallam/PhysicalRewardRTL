Write a Verilog module named `addsub3b` that adds or subtracts (sub input selects) two 3-bit unsigned numbers, registered, with a 4-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [2:0] a, b
  input sub (0=add,1=sub)
  output [3:0] result
