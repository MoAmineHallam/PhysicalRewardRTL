Write a Verilog module named `lfsr13_11`, a 13-bit Galois LFSR with feedback tap mask 0x100d and seed 0x1fff (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [12:0] state.
