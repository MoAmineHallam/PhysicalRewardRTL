Write a Verilog module named `add6b` that adds two 6-bit unsigned numbers, registered, with a 7-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [5:0] a, b
  output [6:0] result
