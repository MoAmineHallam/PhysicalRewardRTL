Write a Verilog module named `mod160_counter` that is a free-running modulo-160 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [7:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,159,0,... wrapping at 160.
