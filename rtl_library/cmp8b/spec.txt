Write a Verilog module named `cmp8b`, a registered 8-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [7:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
