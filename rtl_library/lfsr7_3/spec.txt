Write a Verilog module named `lfsr7_3`, a 7-bit Galois LFSR with feedback tap mask 0x41 and seed 0x7f (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [6:0] state.
