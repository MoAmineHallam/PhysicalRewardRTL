Write a Verilog module named `lfsr4_0`, a 4-bit Galois LFSR with feedback tap mask 0x9 and seed 0xf (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [3:0] state.
