Write a Verilog module named `mod5_counter` that is a free-running modulo-5 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [2:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,4,0,... wrapping at 5.
