Write a Verilog module named `echo8b` with inputs `clk`, active-low `rst_n`, and 8-bit `x`, and a 16-bit registered output `y`. Delay `x` by three clock cycles through a chain of registers and drive `y` with the zero-extended delayed value. Clear all registers to 0 on `!rst_n`.
