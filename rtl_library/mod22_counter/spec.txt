Write a Verilog module named `mod22_counter` that is a free-running modulo-22 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [4:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,21,0,... wrapping at 22.
