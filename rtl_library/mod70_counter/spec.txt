Write a Verilog module named `mod70_counter` that is a free-running modulo-70 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [6:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,69,0,... wrapping at 70.
