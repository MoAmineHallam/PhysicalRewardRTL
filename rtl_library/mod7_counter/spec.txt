Write a Verilog module named `mod7_counter` that is a free-running modulo-7 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [2:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,6,0,... wrapping at 7.
