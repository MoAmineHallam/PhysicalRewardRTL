Write a Verilog module named `addsub12b` that adds or subtracts (sub input selects) two 12-bit unsigned numbers, registered, with a 13-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [11:0] a, b
  input sub (0=add,1=sub)
  output [12:0] result
