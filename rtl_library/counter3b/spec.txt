Write a Verilog module named `counter3b`, a 3-bit free-running binary up-counter.

Ports:
  input clk
  input rst_n (active-low sync reset)
  output [2:0] count

Behavior:
  - Registered. On !rst_n count<=0. Else count<=count+1 (wraps at 2^3).
