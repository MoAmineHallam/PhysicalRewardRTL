Write a Verilog module named `add9b` that adds two 9-bit unsigned numbers, registered, with a 10-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [8:0] a, b
  output [9:0] result
