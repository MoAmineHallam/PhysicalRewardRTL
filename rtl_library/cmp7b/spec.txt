Write a Verilog module named `cmp7b`, a registered 7-bit magnitude comparator.

Ports:
  input clk, rst_n
  input [6:0] a, b
  output gt, eq, lt

Behavior: gt=(a>b), eq=(a==b), lt=(a<b), all registered.
