Write a Verilog module named `sub10b` that subtracts two 10-bit unsigned numbers, registered, with a 11-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [9:0] a, b
  output [10:0] result
