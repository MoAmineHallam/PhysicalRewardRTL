Write a Verilog module named `addsub4b` that adds or subtracts (sub input selects) two 4-bit unsigned numbers, registered, with a 5-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [3:0] a, b
  input sub (0=add,1=sub)
  output [4:0] result
