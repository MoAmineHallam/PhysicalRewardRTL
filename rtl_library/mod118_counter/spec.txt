Write a Verilog module named `mod118_counter` that is a free-running modulo-118 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [6:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,117,0,... wrapping at 118.
