Write a Verilog module named `add2b` that adds two 2-bit unsigned numbers, registered, with a 3-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [1:0] a, b
  output [2:0] result
