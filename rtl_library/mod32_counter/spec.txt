Write a Verilog module named `mod32_counter` that is a free-running modulo-32 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [4:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,31,0,... wrapping at 32.
