Write a Verilog module named `counter4b`, a 4-bit free-running binary up-counter.

Ports:
  input clk
  input rst_n (active-low sync reset)
  output [3:0] count

Behavior:
  - Registered. On !rst_n count<=0. Else count<=count+1 (wraps at 2^4).
