Write a Verilog module named `updown5b`, a 5-bit up/down counter.

Ports:
  input clk, rst_n
  input dir (0=up, 1=down)
  output [4:0] count

Behavior:
  - Registered. On !rst_n count<=0. dir=0: +1, dir=1: -1.
