Write a Verilog module named `addsub10b` that adds or subtracts (sub input selects) two 10-bit unsigned numbers, registered, with a 11-bit result (top bit = carry/borrow).

Ports:
  input clk, rst_n
  input [9:0] a, b
  input sub (0=add,1=sub)
  output [10:0] result
