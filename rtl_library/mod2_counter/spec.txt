Write a Verilog module named `mod2_counter` that is a free-running modulo-2 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [0:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,1,0,... wrapping at 2.
