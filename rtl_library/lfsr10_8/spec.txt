Write a Verilog module named `lfsr10_8`, a 10-bit Galois LFSR with feedback tap mask 0x240 and seed 0x3ff (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [9:0] state.
