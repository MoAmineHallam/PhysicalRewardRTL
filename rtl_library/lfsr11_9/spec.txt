Write a Verilog module named `lfsr11_9`, a 11-bit Galois LFSR with feedback tap mask 0x500 and seed 0x7ab (on !rst_n). Each cycle: shift right; if outgoing LSB is 1, XOR the shifted value with the tap mask. Output [10:0] state.
