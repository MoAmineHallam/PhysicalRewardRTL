Write a Verilog module named `mod134_counter` that is a free-running modulo-134 counter.

Ports:
  input  clk
  input  rst_n   active-low synchronous reset
  output [7:0] count

Behavior:
  - Registered on posedge clk.
  - On !rst_n: count <= 0.
  - Counts 0,1,...,133,0,... wrapping at 134.
